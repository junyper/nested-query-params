{{! ember-template=;omt}}
<fieldset>
  <legend>
    Filters

    <LinkTo
      title="clear"
      aria-label="clear filters"
      @route={{route-name}}
      @query={{@defaults}}
    >
      ×
    </LinkTo>
  </legend>
  {{#each @options as |option|}}
    <FilterCheckbox
      @name={{@name}}
      @value={{option.value}}
      @label={{option.label}}
      @checked={{if (array-contains @selections option.value) true null}}
      @onChange={{fn this.handleChange option.value}}
    />
  {{/each}}
</fieldset>