{{page-title this.filter}}

<DebugFilters @query={{this.q}} @filters={{this.f}} />
{{log this.selections}}
<FilterFieldset
  @defaults={{this.defaults}}
  @options={{this.options}}
  @selections={{this.selections}}
  @name={{this.name}}
  @onChange={{this.handleFilterChange}}
/>

{{outlet}}