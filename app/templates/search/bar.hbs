{{page-title "Bar"}}

<DebugFilters @query={{this.q}} @filters={{this.bar}} />

<FilterFieldset
  @defaults={{this.defaults}}
  @options={{this.options}}
  @selections={{this.selections}}
  @name={{this.name}}
  @onChange={{this.handleFilterChange}}
/>

{{outlet}}